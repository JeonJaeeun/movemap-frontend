import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: "#fff",
  },

  // 오늘의 운동 기록
  pageTitle: {
    fontSize: 24,
    marginBottom: 16,
    color: "#000",
  },
});
